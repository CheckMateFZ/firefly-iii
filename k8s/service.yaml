apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: app
    version: green # initially routes traffic to blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
